# ============================================
# 应用基础配置
# ============================================

# 环境
NODE_ENV=production

# 后端端口
PORT=3000

# 时区配置 (Docker 容器时区)
TZ=Asia/Shanghai

# ============================================
# 数据库配置
# ============================================

# SQLite 数据库路径 (Docker 容器内路径)
DATABASE_URL=file:/app/data/app.db

# ============================================
# JWT 配置
# ============================================

# JWT 密钥 (请务必修改为强密码)
JWT_SECRET=your-super-secret-jwt-key-change-me-in-production

# JWT 过期时间
JWT_EXPIRES_IN=7d

# ============================================
# CORS 配置
# ============================================

# 允许的前端域名
CORS_ORIGIN=http://localhost

# ============================================
# 前端配置
# ============================================

# 前端 API Base URL (开发环境使用，Docker 环境自动使用 /api)
# 开发环境示例: http://localhost:3000/api
# Docker 生产环境使用相对路径 /api (通过 nginx 代理)
VITE_API_BASE_URL=/api

# 前端对外暴露端口
WEB_PORT=8083

# 前端 HTTPS 端口 (仅生产环境)
WEB_SSL_PORT=443

# ============================================
# 日志配置
# ============================================

# 日志级别: error, warn, info, http, verbose, debug, silly
LOG_LEVEL=info

# ============================================
# 限流配置
# ============================================

# 限流时间窗口 (毫秒)
RATE_LIMIT_WINDOW_MS=900000

# 限流最大请求数
RATE_LIMIT_MAX=100

# ============================================
# 其他配置
# ============================================

# 添加其他需要的环境变量
